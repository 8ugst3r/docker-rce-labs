# Wordpress Vulnerability
https://www.exploit-db.com/exploits/52393

## 1. docker installation:
sudo apt update && sudo apt -y full-upgrade
sudo apt install curl gnupg2 apt-transport-https software-properties-common ca-certificates
curl -fsSL https://download.docker.com/linux/debian/gpg | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/docker-archive-keyring.gpg
echo "deb [arch=amd64] https://download.docker.com/linux/debian bullseye stable" | sudo tee /etc/apt/sources.list.d/docker.list
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io
sudo usermod -aG docker $USER
sudo curl -L "https://github.com/docker/compose/releases/download/1.29.2/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
docker-compose version

docker-compose up -d

http://localhost:8080
http://localhost:8080/wp-admin
cd ~/git/docker-rce-labs/first_lab/wp-content/plugins
docker-compose restart wordpress

add plugin manually:

cd <lab-path>/wp-content/plugins
wget https://downloads.wordpress.org/plugin/ultimate-member.2.6.6.zip
unzip ./ultimate-member.2.6.6.zip

activate in localhost:8080/wp-admin/plugins.php


